apiVersion: 1

apps:
  - type: 'acme-dynamicscenes-app'
    org_id: 1
    org_name: 'ACME'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
      isApiKeySet: true
    secureJsonData:
      apiKey: secret-key
